---
title: Stat
description: Display a single metric with title, value, and description.
---

Display a single metric with title, value, and description. Stats can be grouped horizontally or vertically and decorated with figures and actions.

<StatClassesTable />

## Default Stat

Wrap a single `stat` in a `stats` container.

<HtmlPreview name="stat-default" />

## Multiple Stats

Group several stats side by side with dividers between them.

<HtmlPreview name="stat-multiple" />

## Stats with Figures

Add a `stat-figure` element to render an icon next to the value.

<HtmlPreview name="stat-figure" />

## Vertical Stats

Use `stats-vertical` to stack the stats on top of each other.

<HtmlPreview name="stat-vertical" />

## Stats with Actions

Use `stat-actions` to attach buttons or links beneath the value.

<HtmlPreview name="stat-actions" />
