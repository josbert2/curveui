---
title: Collapse
description: Collapses show or hide a region of content via a trigger button.
---

Collapse is a simple visibility primitive: pair a trigger button with a `collapse` element and toggle the `hidden` class to expand or close the region.

<CollapseClassesTable />

## Default

The minimal markup pairs a button with a `collapse` panel. The visible state has no extra modifiers.

<HtmlPreview name="collapse-default" />

## Card Style

Render the collapse as a card by adding background, border and shadow utilities directly on the panel.

<HtmlPreview name="collapse-card" />

## Hidden State

Add the `hidden` class to start the panel collapsed. Toggle it with JavaScript when the trigger is clicked.

<HtmlPreview name="collapse-hidden" />

## Group of Collapses

Stack multiple collapses to build a lightweight FAQ or details list.

<HtmlPreview name="collapse-group" />
