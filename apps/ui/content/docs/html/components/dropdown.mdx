---
title: Dropdown
description: Dropdowns reveal a list of actions or options anchored to a trigger.
---

Dropdowns are floating menus anchored to a trigger button. The base `dropdown-menu` is hidden by default; the examples below force it visible (`opacity-100`) for documentation purposes.

<DropdownClassesTable />

## Default

The minimal dropdown wraps a button and a `dropdown-menu` list. Each option uses `dropdown-item`.

<HtmlPreview name="dropdown-default" />

## With Icons

Place an SVG icon before the label to communicate the action.

<HtmlPreview name="dropdown-with-icons" />

## With Header and Footer

Use `dropdown-header` and `dropdown-footer` to add metadata or summary actions.

<HtmlPreview name="dropdown-with-header" />

## With Titles

Group items with `dropdown-title`. Use `dropdown-active` and `dropdown-disabled` to style states.

<HtmlPreview name="dropdown-with-title" />
