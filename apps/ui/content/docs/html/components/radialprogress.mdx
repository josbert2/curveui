---
title: Radial Progress
description: Show progress as a circular ring around a label.
---

Show progress as a circular ring around a label. Drive the value with the `--value` CSS variable.

<RadialprogressClassesTable />

## Default Radial Progress

Use `radial-progress` and set the inline `--value` between 0 and 100.

<HtmlPreview name="radialprogress-default" />

## Radial Progress Colors

Combine with text utilities like `text-primary`, `text-success`, etc. The ring inherits `currentColor`.

<HtmlPreview name="radialprogress-colors" />

## Sizes

Override `--size` to control the diameter. Combine with `--thickness` for thicker rings.

<HtmlPreview name="radialprogress-sizes" />

## Thickness

Customize the ring thickness with the `--thickness` variable.

<HtmlPreview name="radialprogress-thickness" />

## Filled Variant

Add a background and border to highlight the entire disc, not just the ring.

<HtmlPreview name="radialprogress-filled" />
